# XDP64

A pretty simple computer based on the PDP10 with a 64 bit word.

The emulator is made for Windows explicitly, it might need some porting work done for Linux. To compile XDP64.c, 
```gcc xdp64.c -o xdp64 -lws2_32```
